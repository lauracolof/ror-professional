<p>Intentos: <%= session[:intentos] %> </p>

<%= form_with model: @photo do |form| %>

  <div>
  <%= form.label :title %>
  <%= form.text_field :title %>
  </div>

  <div>
    <%= form.check_box :is_public %>
  </div>

  <div>
    <%= form.collection_select(:todo_id, Todo.all, :id, :title, {include_blank: true}) %>
  </div>

  <div>
  <%= form.label :image_url %>
  <%= formf.url_field :image_url %>
  </div>

  <%= formf.submit "Submit" %>
  
<% end %>
  